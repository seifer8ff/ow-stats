<hr>
{{#if heroPage}}
	<h6 class="section-label text-uppercase">Hero VS All</h6>
	{{else}}
	<h6 class="section-label text-uppercase">Hero VS Selected</h6>
{{/if}}
{{#if this.stats.playtime}}
	<section class="hero-stat-section hero-playtime">
		<h5>Playtime</h5>
		<div class="stat-bar">{{this.stats.playtime}}</div>
	</section>
{{/if}}
{{#if this.stats.medals}}
	<section class="hero-stat-section hero-medals">
		<h5>Medals</h5>
		<div class="stat-bar">{{this.stats.medals}}</div>
	</section>
{{/if}}
{{#if this.stats.elims}}
	<section class="hero-stat-section hero-elims">
		<h5>Eliminations Per Life</h5>
		<div class="stat-bar">{{this.stats.elims}}</div>
	</section>
{{/if}}
{{#if this.heroDamage}}
	<section class="hero-stat-section hero-damage">
		<h5>Hero Damage Done</h5>
		<div class="stat-bar">{{this.heroDamage}}</div>
	</section>
{{/if}}
{{#if this.stats.healing}}
	<section class="hero-stat-section hero-healing">
		<h5>Healing</h5>
		<div class="stat-bar">{{this.stats.healing}}</div>
	</section>
{{/if}}